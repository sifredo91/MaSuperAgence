{% extends 'base.html.twig' %}

{% block body %}

    <div class="container mt-4">
        <h1>Editer le tag</h1>
        {{ include('admin/tag/_form.html.twig', {button: 'Editer'}) }}
    </div>

{% endblock %}